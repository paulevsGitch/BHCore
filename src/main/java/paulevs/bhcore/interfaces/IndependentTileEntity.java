package paulevs.bhcore.interfaces;

/**
 * Interface for tile entities that are not linked to blocks.
 * All tile entities that implement this interface will skip tile entity checks on placement.
 */
public interface IndependentTileEntity {}
